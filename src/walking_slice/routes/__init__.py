"""FastAPI route modules.

Each ``routes/*.py`` module wires one bounded context's HTTP surface; the
modules are introduced by tasks 3.3, 5.4, 6.2, 7.2, 8.3, 10.3, and 12.5 and
mounted by ``walking_slice.app`` (task 15.2).
"""
